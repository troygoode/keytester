{
  "name": "keytester",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "next",
    "start": "next start",
    "build": "next build",
    "export": "npm run build && next export",
    "prettier": "prettier --write {lib,pages}/**/*.js"
  },
  "dependencies": {
    "next": "^8.1.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "engines": {
    "node": "^10.15",
    "npm": "^2"
  },
  "devDependencies": {
    "prettier": "^1.17.1"
  }
}
